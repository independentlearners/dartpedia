name: _
publish_to: none

environment:
  sdk: ^3.8.1 # IMPORTANT: Adjust this to match your Dart SDK version or a compatible range

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0

workspace:
  - cli
  - command_runner
  - wikipedia
